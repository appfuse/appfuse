<web-app id="${project.build.finalName}">
    <jsp fast-jstl="false"/>

    <!--database>
        <jndi-name>jdbc/appfuse</jndi-name>
        <driver type="${jdbc.driverClassName}">
            <url>${jdbc.url}</url>
            <user>${jdbc.username}</user>
            <password>${jdbc.password}</password>
        </driver>
    </database-->
</web-app>

